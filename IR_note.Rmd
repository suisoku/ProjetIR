---
title: "Text Mining of Assemblé Generale Documents"
author: "ZIANI,MASSELIN,DIALLO,ANZID"
date: "March 29, 2019"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```


```{r}

# Traitement réalisé
# &nbsp;
# <.*?>
# 
library(rvest)
library(stringi)


textTreeBase <- read_html("http://www.assemblee-nationale.fr/14/cri/2016-2017/20170123.asp")
textSeance1<- textTreeBase %>% html_nodes("body") %>% html_text() #extraction dans le noeud <body> puis dnas le noeud <p>
#stri_replace_all(text,"", regex = "\r")
#stri_replace_all(text,"", regex = "\t")
#stri_replace_all(text,"", regex = "\n")


```



